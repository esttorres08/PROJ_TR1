# Projeto TR1

Para rodar o projeto sera necessario baixar as seguintes bibliotecas:

```bash
pip install matplotlab
pip install streamlit
pip install numpy
```

depois das bibliotecas serem instaladas rode os seguintes comandos:

para o servidor
```bash
pyhton3 servidor.py
```

para a interface
```bash
streamlit run interface.py
```
